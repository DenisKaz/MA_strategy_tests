# MA Strategy Tests – Анализ отскоков от Moving Averages

Аналитический инструмент для поиска и оценки качества *«ровных отскоков»* от скользящих средних (SMA/EMA) на исторических OHLCV данных криптовалют.

## Быстрый старт (5 минут)

```bash
# 1. Установить зависимости
pip install -r requirements.txt

# 2. Загрузить данные (первый раз, ~5 минут)
python bin/fetch.py --symbols BTC/USDT --years 1

# 3. Запустить анализ (перебор MA 5..233, ~2 минуты)
python bin/analyze.py

# 4. Проверить результаты
cat results/analysis_summary.csv | head -20
```

---

## Что это делает?

### Проблема

На графиках видно, что цена иногда касается скользящей средней **только хвостом свечи** (не телом), а потом уходит от неё. Эти касания — потенциальные точки поворота. Но какие MA периоды дают надёжные сигналы?

### Решение

Проект перебирает все MA периоды (5–233), все типы (SMA, EMA) и все таймфреймы (1m–1d), и для каждой комбинации подсчитывает:
- **Win Rate:** какая часть таких касаний привела к движению на 3%+?
- **Метрики качества:** среднее время до цели, макс. откат, и т.д.

### Результат

CSV таблица типа:

| file | ma_period | ma_type | timeframe | total_events | wins | win_rate_% | avg_time_to_target |
|------|-----------|---------|-----------|--------------|------|------------|-------------------|
| BTC_USDT_1m.csv | 20 | SMA | 1m | 127 | 71 | 55.91 | 14.3 |
| BTC_USDT_1m.csv | 50 | SMA | 1m | 95 | 48 | 50.53 | 15.2 |

---

## Архитектура

```
MA_strategy_tests/
├── src/
│   ├── config.py       # Параметры (биржа, символы, пороги)
│   ├── data_fetcher.py # Загрузка с CCXT, кэширование в CSV
│   └── analyzer.py     # Расчёт MA, детектирование отскоков, метрики
├── bin/
│   ├── fetch.py        # CLI для загрузки
│   └── analyze.py      # CLI для анализа
├── data/               # OHLCV CSV (создаётся автоматически)
└── results/            # Результаты анализа (создаётся автоматически)
```

**Подробное описание:**  
👉 [ARCHITECTURE.md](./ARCHITECTURE.md) — разбор архитектуры, алгоритмов и решений

---

## Установка

### Требования
- Python 3.9+
- pip

### Шаги

```bash
# Клонировать (если из гита)
git clone <repo>
cd MA_strategy_tests

# Создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

---

## Использование

### 1. Загрузка данных

**Первый запуск (скачивает 4 года истории):**
```bash
python bin/fetch.py --symbols BTC/USDT ETH/USDT
```

**Опции:**
```bash
python bin/fetch.py --help

# Примеры:
python bin/fetch.py --symbols BTC/USDT --years 1
python bin/fetch.py --symbols BTC/USDT ETH/USDT SOL/USDT --timeframes 1m 5m 1h
python bin/fetch.py --exchange binance --symbols BTC/USDT
python bin/fetch.py -v  # Подробный лог
```

**Ожидаемый результат:**
```
data/
├── bybit_BTC_USDT_1m.csv      (2M+ строк)
├── bybit_BTC_USDT_5m.csv      (400k строк)
├── bybit_BTC_USDT_1h.csv      (35k строк)
└── bybit_BTC_USDT_1d.csv      (1400 строк)
```

---

### 2. Запуск анализа

**Стандартный запуск (все MA, все данные в data/):**
```bash
python bin/analyze.py
```

**Опции:**
```bash
python bin/analyze.py --help

# Примеры:
python bin/analyze.py --ma-min 5 --ma-max 100
python bin/analyze.py --ma-types SMA  # Только SMA (без EMA)
python bin/analyze.py --target 0.05 --alpha-wick 0.25
python bin/analyze.py -v  # Подробный лог
```

**Ожидаемый результат:**
```
results/
└── analysis_summary.csv      (229 × 2 × 4 = 1832 строк)
```

---

## Примеры команд

### Сценарий 1: Быстрый тест (только BTC, 1 час, мелкие TF)

```bash
# Загрузить
python bin/fetch.py --symbols BTC/USDT --years 0.0027 --timeframes 1m 5m 15m 1h

# Анализировать
python bin/analyze.py --ma-min 20 --ma-max 100
```

### Сценарий 2: Полное исследование (3 инструмента, 4 года)

```bash
# Загрузить
python bin/fetch.py --symbols BTC/USDT ETH/USDT SOL/USDT --years 4

# Анализировать
python bin/analyze.py

# Проверить результаты
python -c "
import pandas as pd
df = pd.read_csv('results/analysis_summary.csv')
print(df.nlargest(20, 'win_rate_%')[['file', 'ma_period', 'timeframe', 'win_rate_%', 'total_events']])
"
```

### Сценарий 3: Дебаг одного MA периода

```bash
# Анализировать только MA=50
python bin/analyze.py --ma-min 50 --ma-max 50 -v
```

---

## Интерпретация результатов

### Колонки в results/analysis_summary.csv

| Колонка | Значение |
|---------|----------|
| **file** | Откуда данные (e.g., BTC_USDT_1m.csv) |
| **ma_period** | Период MA (5–233) |
| **ma_type** | SMA или EMA |
| **timeframe** | Таймфрейм (1m, 5m, 1h, 1d) |
| **total_events** | Всего найдено качественных отскоков |
| **wins** | Из них достигли целевого движения (3%) |
| **losses** | Не достигли |
| **win_rate_%** | wins / total_events × 100 |
| **avg_time_to_target** | Среднее: свечей до цели |
| **median_time_to_target** | Медиана |
| **avg_adverse_max_%** | Среднее макс. отклонение против хода (risk/reward) |

### Пример анализа

```
ma_period=50, ma_type=SMA, timeframe=1h, win_rate=62%, total_events=45

→ Из 45 отскоков от MA50 на 1h, в 28 цена ушла на 3%+
→ Это неплохо! Но нужна проверка на других данных
```

### ⚠️ Важные замечания

1. **Это НЕ торговый сигнал!** На бэктесте на том же данных, которые вы анализировали
2. **Overfitting:** Найденные периоды могут быть специфичны для этого инструмента/таймфрейма
3. **Look-ahead bias:** Анализ использует будущие 200 свечей, в реальности их не знаете!
4. **Не забудьте про комиссии:** 0.1% туда и обратно = 0.2% минус для каждой сделки

**Как использовать результаты:**
- Как идею для дальнейшего backtesting с комиссиями, стопами, временем ввода
- Как сравнение между MA периодами (какие более/менее надёжны)
- Как входные параметры для ML-модели

---

## Параметры конфигурации

### Основные параметры (config.py)

```python
# Что загружать
symbols: ['BTC/USDT', 'ETH/USDT']
timeframes: ['1m', '5m', '15m', '1h', '2h', '4h', '1d']
max_history_years: 4

# Параметры поиска отскоков
ma_period_min: 5
ma_period_max: 233
ma_types: ['SMA', 'EMA']

alpha_wick: 0.30       # Минимум 30% от размера свечи
n_pre: 5               # 5 свечей до без касаний
n_post: 5              # 5 свечей после без касаний

target_pct: 0.03       # Целевое движение 3%
max_lookahead: 200     # Максимум свечей для проверки цели
```

### Как менять

**Вариант 1: Через CLI**
```bash
python bin/analyze.py --ma-min 10 --ma-max 50 --alpha-wick 0.25 --target 0.05
```

**Вариант 2: Отредактировать src/config.py**
```python
AnalysisConfig(
    ma_period_min=10,
    ma_period_max=50,
    ...
)
```

---

## Почему результаты могут вводить в заблуждение

| Риск | Описание | Решение |
|------|---------|---------|
| **Overfitting** | MA найдена на одних данных | Тестировать на других инструментах/периодах |
| **Look-ahead bias** | Проверка 200 свечей вперёд | Использовать только как метрику, не сигнал |
| **Data-snooping** | Перебрали 229 × 2 × 4 = 1832 комбинации | Поправка Бонферрони на множественные тесты |
| **Комиссии** | Анализ не учитывает -0.1–0.2% | Вычесть из win_rate вручную |
| **Slippage** | Реальная цена ≠ OHLCV close | Добавить 0.5–2% к spreads |

---

## Примеры файлов

### data/bybit_BTC_USDT_1m.csv
```
datetime,open,high,low,close,volume
2021-11-16 00:00:00+00:00,61512.0,61600.0,61450.0,61590.0,45.2
2021-11-16 00:01:00+00:00,61590.0,61650.0,61550.0,61600.0,38.1
2021-11-16 00:02:00+00:00,61600.0,61620.0,61500.0,61510.0,41.5
...
```

### results/analysis_summary.csv
```
file,ma_period,ma_type,timeframe,total_events,wins,losses,win_rate_%,avg_time_to_target,median_time_to_target,avg_adverse_max_%
bybit_BTC_USDT_1m.csv,5,SMA,1m,312,142,170,45.51,8.2,5,1.23
bybit_BTC_USDT_1m.csv,5,EMA,1m,298,148,150,49.66,8.5,5,1.15
bybit_BTC_USDT_1m.csv,10,SMA,1m,287,156,131,54.36,9.1,7,0.98
...
```

---

## FAQ

### Q: Как долго идёт загрузка?
**A:** 1 инструмент × 9 таймфреймов × 4 года ≈ 3–5 минут (зависит от сети и rate limits биржи).

### Q: Почему не все символы загрузились?
**A:** Биржа может не поддерживать символ. Проверьте валидные пары в логе или на сайте биржи.

### Q: Могу ли я использовать другую биржу?
**A:** Да! Передайте `--exchange binance` или `--exchange kraken` (должны быть в CCXT).

### Q: Как добавить свой символ?
**A:** `python bin/fetch.py --symbols YOUR/USDT`

### Q: Что если данных меньше 4 лет?
**A:** Скрипт загружает столько, сколько есть. Это OK для бэктестирования.

### Q: Как я могу использовать эти MA сигналы в торговле?
**A:**
1. Не используйте прямо (есть look-ahead bias)
2. Проверьте на другом инструменте / периоде (out-of-sample)
3. Добавьте комиссии, стопы, максимальный риск
4. Backtesting с реальными условиями (spreads, slippage)
5. Paper trading перед реальными деньгами

---

## Отладка

### Проблема: "Символ не найден"
```
✗ Символ НЕ найден: FAKE/USDT
```
**Решение:** Проверьте правильность названия (должно быть в формате BASE/QUOTE).

### Проблема: Rate limit ошибка
```
⚠ Ошибка сети/биржи: 418 I'm a teapot
```
**Решение:** Снизьте --limit с 1000 до 500:
```bash
python bin/fetch.py --limit 500
```

### Проблема: CSV файл пустой
**Решение:** Проверьте лог:
```bash
python bin/fetch.py -v
```

### Проблема: Анализ очень медленный
**Решение:** 
- Ограничьте периоды: `--ma-min 20 --ma-max 100`
- Ограничьте lookahead: [редактировать в config.py]

---

## Расширенное использование

### Программный интерфейс (Python)

```python
from src.config import ExchangeConfig, FetchConfig, AnalysisConfig
from src.data_fetcher import DataFetcher
from src.analyzer import Analyzer

# Загрузить данные
fetch_config = FetchConfig(symbols=['BTC/USDT'], max_history_years=1)
fetcher = DataFetcher(ExchangeConfig(), fetch_config)
fetcher.fetch_and_save()

# Анализировать
analysis_config = AnalysisConfig(ma_period_min=20, ma_period_max=100)
analyzer = Analyzer(analysis_config)
results = analyzer.analyze_all_data()

print(results.nlargest(10, 'win_rate_%'))
```

### Кастомный анализ

Отредактируйте `src/analyzer.py` методы:
- `is_wick_touch()` — пороги касания
- `lookahead_target()` — логика проверки цели
- `analyze_all_data()` — главной цикл

---

## Производительность

| Данные | Операция | Время |
|--------|----------|-------|
| 1M свечей 1m | Загрузка | 5 мин |
| 1M свечей 1m | Анализ (229 MA × 2 type) | 2 мин |
| Всё | Total | ~7 мин |

Для 10M+ свечей рекомендуется:
- Параллелизм (multiprocessing по периодам)
- Кэширование MA (не пересчитывать)
- Parquet вместо CSV

---

## Лицензия

MIT

---

## Помощь и вопросы

1. Проверьте [ARCHITECTURE.md](./ARCHITECTURE.md) для подробного разбора
2. Включите `-v` флаг для подробного лога
3. Проверьте `data/` и `results/` на наличие файлов

---

**Последнее обновление:** November 16, 2025

## Алгоритм анализа

### Определение отскока

1. **Wick Touch** — касание MA только хвостом свечи (не телом):
   - Бычий отскок: тело выше MA, нижняя тень касается MA, длина тени ≥ 30% размера свечи
   - Медвежий отскок: тело ниже MA, верхняя тень касается MA, длина тени ≥ 30% размера свечи

2. **Изолированность** — перед и после касания минимум 5 свечей без других касаний MA

### Проверка результата

- После касания цена должна уйти от закрытия касательной свечи на ≥ 3% (настраивается)
- При этом цена НЕ должна вернуться к цене закрытия касательной свечи
- Проверяется максимум 200 свечей вперёд (или до конца данных)

### Метрики

Для каждого MA (период + тип: SMA/EMA) рассчитываются:
- **total_events** — количество найденных изолированных отскоков
- **win_rate** — процент успешных отскоков (достигнут целевой уровень)
- **wins** — количество выигрышных отскоков
- **losses** — количество неудачных отскоков
- **avg_time_to_target** — среднее количество свечей до достижения цели
- **median_time_to_target** — медианное количество свечей до достижения цели
- **avg_adverse_max** — среднее макс. отклонение против направления

## Установка и запуск

### 1. Требования

- Python 3.9+
- Установить зависимости:

```bash
pip install -r requirements.txt
```

### 2. Загрузить данные

Загрузить OHLCV данные с биржи (по умолчанию: BTC/USDT, последний год, таймфреймы: 1m, 5m, 15m, 1h):

```bash
python bin/fetch.py
```

Примеры:
```bash
# Загрузить несколько пар
python bin/fetch.py --symbols BTC/USDT ETH/USDT

# Загрузить за 2 года с таймфреймами 1m, 5m, 1h
python bin/fetch.py --years 2 --timeframes 1m 5m 1h

# Загрузить с другой биржи (если поддерживается CCXT)
python bin/fetch.py --exchange binance --symbols BTC/USDT

# Увеличить лимит свечей на запрос (для более быстрой загрузки)
python bin/fetch.py --limit 1000
```

### 3. Анализировать данные

Запустить анализ и найти оптимальные MA периоды для отскоков:

```bash
python bin/analyze.py
```

Примеры:
```bash
# Анализировать MA периоды 5-100
python bin/analyze.py --ma-min 5 --ma-max 100

# Только SMA (без EMA)
python bin/analyze.py --ma-types SMA

# Изменить целевое движение на 5% и хвост на 25%
python bin/analyze.py --target 0.05 --alpha-wick 0.25

# Подробный лог
python bin/analyze.py -v
```

## Результаты

Результаты сохраняются в папке `results/`:

- `analysis_results.csv` — основной результат: все MA, их метрики, отсортировано по win_rate

Пример строки результата:
```
symbol,timeframe,ma_type,period,total_events,win_rate,wins,losses,avg_time_to_target,median_time_to_target,avg_adverse_max
BTC/USDT,1h,SMA,50,25,68.00,17,8,12.5,11.0,0.0145
```

## Параметры конфигурации

Все параметры можно менять через CLI-аргументы. Основные:

### Загрузка данных (`bin/fetch.py`)
- `--symbols` — торговые пары (по умолчанию: BTC/USDT)
- `--timeframes` — таймфреймы (по умолчанию: 1m 5m 15m 1h)
- `--years` — количество лет истории (по умолчанию: 1)
- `--limit` — свечей на запрос (по умолчанию: 1000)
- `--exchange` — биржа (по умолчанию: bybit)

### Анализ (`bin/analyze.py`)
- `--ma-min`, `--ma-max` — диапазон периодов (5–233)
- `--ma-types` — типы MA: SMA, EMA или оба
- `--alpha-wick` — минимальная длина хвоста (по умолчанию: 0.30)
- `--n-pre`, `--n-post` — свечи до/после касания (по умолчанию: 5)
- `--target` — целевое движение цены (по умолчанию: 0.03)
- `--max-lookahead` — максимум свечей вперёд (по умолчанию: 200)
- `--min-events` — минимум событий (по умолчанию: 10)
- `-v` — подробный лог

## Примеры workflow

### Полный анализ за день

```bash
# Загрузить данные за 2 года (один раз)
python bin/fetch.py --symbols BTC/USDT ETH/USDT --years 2 --timeframes 1m 5m 15m 1h

# Анализировать с дефолтными параметрами
python bin/analyze.py

# Смотреть результаты
cat results/analysis_results.csv
```

### Поиск оптимальных параметров

```bash
# Сначала узкий диапазон MA с дефолтным таймфреймом
python bin/analyze.py --ma-min 5 --ma-max 50 --min-events 5

# Потом — более точный анализ по SMA
python bin/analyze.py --ma-types SMA --target 0.05 --alpha-wick 0.25
```

## FAQ

**Q: Почему я получаю только ~1000 строк?**
A: Это лимит на один запрос. Скрипт делает несколько запросов автоматически. Если всё равно мало — увеличьте `--years`.

**Q: Как скачивать только таймфреймы < 1d?**
A: По умолчанию для таймфреймов < 1d используется история за N лет. Для >= 1d скачивается всё с начала доступной истории.

**Q: Что если анализ долгий?**
A: Увеличьте `--min-events` чтобы пропускать неинформативные MA.

## Лицензия

MIT

### 2. Загрузить данные

Загрузить BTC/USDT за 1 год во всех таймфреймах < 1d:

```bash
python bin/fetch.py --exchange bybit --symbols BTC/USDT --years 1
```

Загрузить несколько пар:

```bash
python bin/fetch.py --symbols BTC/USDT ETH/USDT --years 2
```

Полные опции:

```bash
python bin/fetch.py --help
```

```
--exchange EXCHANGE        ID биржи (bybit, binance, kraken)
--symbols SYMBOLS [...]    Торговые пары (BTC/USDT ETH/USDT)
--timeframes TF [...]      Таймфреймы (1m 5m 1h 1d)
--years YEARS              Количество лет истории (по умолчанию: 1)
--limit LIMIT              Свечей на запрос (по умолчанию: 1000)
--max-candles N            Максимум свечей всего (по умолчанию: 500000)
-v, --verbose              Подробный лог
```

**Примеры:**

```bash
# Загрузить BTC за 2 года
python bin/fetch.py --symbols BTC/USDT --years 2

# Загрузить ETH и BTC за 1 год, только мелкие TF
python bin/fetch.py --symbols BTC/USDT ETH/USDT --timeframes 1m 5m 15m 1h

# С подробным логом
python bin/fetch.py -v
```

### 3. Проанализировать данные

Анализ с MA периодами 20, 50, 200:

```bash
python bin/analyze.py --ma 20 50 200
```

Полные опции:

```bash
python bin/analyze.py --help
```

```
--ma MA [...]              Периоды скользящих средних (по умолчанию: 20 50 200)
--lookhead N               Количество свечей для анализа (по умолчанию: 5)
--min-change CHANGE_%      Минимальное изменение цены (по умолчанию: 0.5%)
-v, --verbose              Подробный лог
```

**Примеры:**

```bash
# Анализ с другими MA
python bin/analyze.py --ma 12 26 50

# Анализ с 10-свечным lookahead
python bin/analyze.py --lookhead 10 --min-change 1.0

# Подробный лог
python bin/analyze.py -v
```

---

## 📊 Рабочий процесс

### Типичный сценарий

```bash
# Шаг 1: Загрузить исторические данные (редко, один раз или периодически)
python bin/fetch.py --symbols BTC/USDT ETH/USDT --years 1

# Шаг 2: Дождаться завершения (может занять время!)
# Данные сохранятся в ./data/*.csv

# Шаг 3: Проанализировать данные
python bin/analyze.py --ma 20 50 200

# Шаг 4: Проверить результаты
# Сводка: ./results/analysis_summary.csv
# Детали: ./results/*_analysis.csv
```

### Директории

- **`./data/`** — OHLCV CSV файлы (созданы fetch.py)
  - Формат: `bybit_BTC_USDT_1m.csv`, `bybit_ETH_USDT_1h.csv` и т.д.
  
- **`./results/`** — результаты анализа (созданы analyze.py)
  - `analysis_summary.csv` — сводка по всем файлам
  - `*_analysis.csv` — детальный анализ каждого файла

---

## 🔧 Использование как модули (programmatic API)

### Загрузить данные

```python
from src.config import ExchangeConfig, FetchConfig
from src.data_fetcher import DataFetcher

exchange_config = ExchangeConfig(exchange_id='bybit')
fetch_config = FetchConfig(
    symbols=['BTC/USDT', 'ETH/USDT'],
    timeframes=['1m', '5m', '1h'],
    years_of_history=2,
)

fetcher = DataFetcher(exchange_config, fetch_config)
fetcher.fetch_and_save()
```

### Проанализировать данные

```python
from src.config import AnalysisConfig
from src.analyzer import Analyzer

analysis_config = AnalysisConfig(
    moving_average_periods=[20, 50, 200],
    min_price_change=0.5,
)

analyzer = Analyzer(analysis_config)
results_df = analyzer.analyze_all_data()
print(results_df)
```

### Анализ одного файла

```python
from pathlib import Path

result, df_analyzed = analyzer.analyze_file(Path('data/bybit_BTC_USDT_1h.csv'))
print(result)
# Вывод: {
#   'file': 'bybit_BTC_USDT_1h.csv',
#   'rows_loaded': 8760,
#   'rows_analyzed': 8760,
#   'total_signals': 1203,
#   'wins': 621,
#   'losses': 582,
#   'win_rate_%': 51.62,
#   ...
# }
```

---

## 📈 Индикаторы и метрики

### Moving Average (MA)

Простая скользящая средняя (SMA) за N свечей. Используется для выявления тренда и сигналов пересечения.

```
MA(20) = средняя цена close за последние 20 свечей
MA(50) = средняя цена close за последние 50 свечей
...
```

### Crossover сигналы

- **Бычий** (1): быстрая MA > медленная MA  
- **Медвежий** (-1): быстрая MA < медленная MA  
- **Нейтральный** (0): быстрая MA ≈ медленная MA

**Пример:** MA(20) > MA(50) = сигнал на покупку

### Анализ реакции цены

Для каждого сигнала проверяется, как цена среагировала за N свечей вперёд:

- **Выигрыш** (trade_result = 1): сигнал был прибыльным
- **Потеря** (trade_result = -1): сигнал был убыточным
- **Нейтральный** (trade_result = 0): изменение < min_price_change

**Win Rate** = (Выигрыши / Всего сигналов) × 100%

---

## ⚙️ Конфигурация

### `src/config.py`

Все параметры хранятся в `@dataclass` объектах. Можно переопределять:

```python
from src.config import ExchangeConfig, FetchConfig, AnalysisConfig

# Переопределить стандартные значения
exchange_config = ExchangeConfig(
    exchange_id='binance',           # вместо 'bybit'
    enable_rate_limit=True,
)

fetch_config = FetchConfig(
    symbols=['ETH/USDT', 'SOL/USDT'],
    timeframes=['5m', '15m', '1h'],
    limit_per_request=500,           # вместо 1000
    years_of_history=3,              # вместо 1
)

analysis_config = AnalysisConfig(
    moving_average_periods=[12, 26, 50],  # вместо [20, 50, 200]
    min_price_change=1.0,                 # вместо 0.5
)
```

---

## 🔍 Результаты анализа

### `analysis_summary.csv`

Сводка по всем анализируемым файлам:

| file | rows_loaded | rows_analyzed | total_signals | wins | losses | win_rate_% | avg_winning_return_% | avg_losing_return_% |
|------|-------------|---------------|---------------|------|--------|------------|----------------------|----------------------|
| bybit_BTC_USDT_1m.csv | 526000 | 525980 | 8234 | 4123 | 4111 | 50.07 | 0.234 | -0.231 |
| bybit_BTC_USDT_5m.csv | 105200 | 105196 | 1643 | 834 | 809 | 50.76 | 0.456 | -0.431 |

### `*_analysis.csv`

Детальный анализ каждого файла с колонками:

- `ts`, `open`, `high`, `low`, `close`, `volume` — исходные OHLCV
- `MA_20`, `MA_50`, `MA_200` — значения скользящих средних
- `signal` — сигнал (-1, 0, 1)
- `future_return` — изменение цены за lookahead свечей (%)
- `trade_result` — результат сделки (-1, 0, 1)

---

## 🐛 Отладка и логирование

Включить подробный лог:

```bash
python bin/fetch.py -v
python bin/analyze.py -v
```

Или программно:

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

---

## 📦 Зависимости

- **ccxt** — криптовалютные биржи API
- **pandas** — работа с данными (DataFrame)
- **numpy** — численные операции

```bash
pip install ccxt pandas numpy
```

---

## 🎯 Дальнейшее развитие

Возможные улучшения:

- [ ] Поддержка более сложных индикаторов (EMA, RSI, MACD, Bollinger Bands)
- [ ] Backtesting с комиссиями и slippage
- [ ] Оптимизация параметров (grid search, genetic algorithm)
- [ ] Сохранение прогресса загрузки для возобновления
- [ ] Веб-интерфейс для просмотра результатов
- [ ] Интеграция с реальной торговлей (бумажные деньги)
- [ ] Поддержка live данных

---

## 📝 Лицензия

MIT

---

## 💬 Вопросы и поддержка

Если возникли вопросы:

1. Проверьте, установлены ли все зависимости: `pip install -r requirements.txt`
2. Включите подробный лог: `python bin/fetch.py -v`
3. Убедитесь, что биржа доступна и символы корректны
4. Проверьте доступное место на диске для CSV файлов
